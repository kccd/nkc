mixin renderParagraph(node)
  -
    var style='';
    var {textAlign} = node.attrs;
    if(textAlign) {
      style += `text-align: ${textAlign};`;
    }
    console.log(style);
  p(style=style)
    if node.content
      +loopRender(node.content)
