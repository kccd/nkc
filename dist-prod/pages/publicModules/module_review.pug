.thread-review-info
  -let {reviewPid, reviewType, reviewReason} = reviewData || {};
  if permission("review") && reviewPid
    h4 内容未审核
    if reviewReason
      p= "送审理由：" + reviewReason
    | 通过请点击
    button.btn.btn-xs.btn-default(onclick=`reviewPost('${reviewPid}')`) 通过
    | &nbsp;  按钮，不通过请点击
    if reviewType === "post"
      button.btn.btn-xs.btn-default(onclick=`NKC.methods.disabledPosts('${reviewPid}')`) 退修或删除
    else
      button.btn.btn-xs.btn-default(onclick=`NKC.methods.disabledPosts('${reviewPid}')`) 退修或删除
    | 按钮。
    a.thread-review-link(href=`/review` target="_blank") 待审核列表
  else
    | 审核中
