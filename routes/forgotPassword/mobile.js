const Router = require('koa-router');
const router = new Router();

router
  .get('/', async (ctx, next) => {

  })
  .post('/', async (ctx, next) => {

  })
  .patch('/', async (ctx, next) => {

  });

module.exports = router;