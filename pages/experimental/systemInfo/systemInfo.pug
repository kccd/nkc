extends ../../bootstrap_base

block title
  -const mainContainer1400 = true;
  link(href=`/experimental/index.css?v=${startTime}` rel='stylesheet')
  title 系统通知
  style.
    .info-panel-content{
      margin-bottom: 3rem;
    }
    .info-panel-content span{
      font-size: 1.2rem;
      display: block;
      padding: 1rem;
      background-color: #d9e1e8;
      margin-bottom: 0.5rem;
    }
    .info-panel-content textarea{
      margin-bottom: 0.5rem;
    }
block content
  #__VUE_DATA(style="display: none")!=JSON.stringify({systemMessages: data.systemMessages, roles: data.roles, grades: data.grades})
  .container-fluid.max-width
    .row
      include ../nav
      #app

block scripts
  include ../../publicModules/moment
  include ../../publicModules/strToHTML
  +includeJS("/experimental/systemInfo/systemInfo.js")
