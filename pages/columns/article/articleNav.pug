//- 页头的文章导航路径和右边文号,浏览数,回复,收藏设置
//-文章类型 专栏名 —— 分类 - 小分类 ------------
mixin articleNav(data)
  +includeCSS('/columns/article/articleNav.css')
  header
    p.path-navigation.hidden-xs
      //- a.jump(href=`/m`) 专栏
      //- span.nav-common >
      a.jump(href=`/m/${data.column._id}`)=data.column.name
      span.nav-common >
      if data.mainCategory
        for category,i in data.mainCategory
          a.jump(href=`/m/${data.column._id}?c=${category._id}`)=category.name
          if data.mainCategory.length !== i+1
            span.nav-common >
      else 
        a.jump(href=`/m/${data.column._id}`) 全部
    if data.auxiliaryCategory
      p.path-navigation.m-l-5
        span  辅分类:
        for category,i in data.auxiliaryCategory
          a.jump(href=`/m/${data.column._id}?c=${category._id}`)=category.name 
          if data.auxiliaryCategory.length !== i+1
            span.m-l-1.nav-common |
      
    p.info-list
      span.m-r-1
        span 文号
        span=data.thread.oc  
      span.m-r-1
        span.m-r-1.fa.fa-eye(title="浏览数")=data.thread.hits 
        span.m-r-1.fa.fa-comment(title='回复数' onclick="window.location.href='#container'")=data.thread.count  || data.thread.comment
      span.m-r-1
        span 收藏
        span=data.collectedCount 
      span.fa.fa-cog.m-r-1(onclick="showSetUp()")