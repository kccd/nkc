mixin mixinColumnContribute(user, column, columnToolStyle)
  .column-tool-container.column-contribute-body(style=columnToolStyle)
    if !user
      .column-contribute.thread(onclick='RootApp.openLoginPanel()')
    else if user.uid !== column.uid
      a.column-contribute.thread(href=`/m/${column._id}/contribute` target='_blank')
    else
      a.column-contribute.thread(href=`/editor?toColumn=true`)
    if !user || user.uid !== column.uid
      p 通过审核即可在本专栏显示
    else
      p 发表文章并同步到本专栏

