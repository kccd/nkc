mixin mixinColumnArticleContribute(user, column, columnToolStyle)
    .column-tool-container.column-contribute-body(style=columnToolStyle)
        if !user
            .column-contribute.article(onclick='NKC.methods.toLogin()')
        else if user.uid !== column.uid
            a.column-contribute.article(href=`/m/${column._id}/contribute` target='_blank')
        else
            a.column-contribute.article(href=`/column/editor?source=column&mid=${column._id}`)
        if !user || user.uid !== column.uid
            p 通过审核即可在本专栏显示
        else
            p 发表独立文章并同步到本专栏

