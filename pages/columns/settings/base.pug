extends ../columnBase
block content
  .container-fluid.max-width
    .column
      .row
        .col-xs-12.col-md-12.box-shadow-panel.m-b-1
          div
            .column-settings-nav
              a(href=`/m/${data.column._id}/settings` class=data.highlight==="info"?"active":"") 资料设置
              a(href=`/m/${data.column._id}/settings/post` class=data.highlight==="post"?"active":"") 内容管理
              a(href=`/m/${data.column._id}/settings/reminder`) 投稿通知
              a(href=`/m/${data.column._id}/settings/change`) 专栏转让
            hr.m-t-05
            div
              block settingsContent



