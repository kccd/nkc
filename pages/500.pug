extends bootstrap_base.pug

block title
  title 500 Int Serv Err 服务器错误
  style.
    .code {
      padding: 2px 4px;
      font-size: 90%;
      color: #c7254e;
      background-color: #f9f2f4;
      border-radius: 4px;
    }
block content
  .container
    .row
      .col-sm-12
        h1 Sorry! 抱歉
        p!= `request to path <code class="errUrl">${data.url}</code> encountered an error. If you think this shouldn't happen, please contact the Site Admin.`
        p!= `请求的网址<code class="errUrl">${data.url}</code> 出错。如果您认为这个错误不应该发生，请通知网站管理员。`
        hr
        p 信息 message:
        pre.intpad.zipline(style='background-color:#dceeff;') #{data.message.toString()}
        p(style="color:#999;") #{server.name}
block scripts
  script.
    $('.errUrl').text(window.location.pathname);
