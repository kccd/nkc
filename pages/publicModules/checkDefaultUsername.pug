-const reg = new RegExp(`^${remoteState.serverSettings.websiteCode}-`);
if data.user && (reg.test(data.user.username) || !data.user.username)
  .col-xs-12.m-b-1
    .home-warning
      .fa.fa-exclamation-circle
      | 你还未设置用户名，点击&nbsp;
      a(href=`/u/${data.user.uid}/settings` target="_blank") 这里
      | &nbsp;去设置。
