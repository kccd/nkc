extends scratch.pug

block head
  include styles.pug
  title 404 Not Found
block  body
  h1 Sorry
  :markdown
    path `#{data.url}` does not exist | can not be listed | is not available.
  br
  img(src='/site_specific/owl_bath.jpg')
  p(style="color:#999;") #{server.name}
