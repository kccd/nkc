extends ../bootstrap_base
block title
  title=`开卷考试`
  +includeCSS('/exam/takeExam.css')
block content
  .container-fluid.max-width(id='take-exam' v-cloak).box-shadow-panel
    .exam-container
      Public-Paper(pid=data.pid)
block scripts
  +includeJS('/exam/takeExam.js')
