extends ../bootstrap_base
block title
  title=`开卷考试说明`
  +includeCSS('/exam/public.css')
block content
  .container-fluid.max-width
    .examNotes
      h2.m-b-1 考试须知
      .pre-content.m-b-1!=markdown_safe(data.examNotice)
      a.btn.btn-default.fund-terms-button(href=`/exam/paper?cid=${data.cid}&userFrom=register`) 参加考试
